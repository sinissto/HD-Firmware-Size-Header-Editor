Currently, app do successfully header edit of selected .bin file and that is exacly what I want. Now, I want to build feature where user is able to choose a folder that contains multiple .bin files. Requirement is that feature must go trough all files in the selected folder and do the same thing that is currently done on individual .bin file. Before files header edit it must make a copy of the folder at the same path as selected folder with same name with addition "\*\_EDITED". Edit must not be automatic but initiated by user click on button, with name of selected folder above it. Error handling is required, do as best as you can to predict posible problems.
Feature must have separate window along side with current window where individual .bin file have been edited at the frontend of the app
Create plan that accopmlish above and save that plan in /features_plan with appropriate name
